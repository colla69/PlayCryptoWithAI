node_modules/

# Secrets — never commit
.env
.env.local
.env.*.local
.env.live

# Runtime logs (not trade history)
logs/*
data/*

# Editor / OS
.idea/
.vscode/
.DS_Store
*.iml

# Resumed Copilot transcript (reference only, not source)
resume
Archive/
Archive.zip

# TSM soak container state (isolated from production data/ and logs/)
data-soak/
logs-soak/
