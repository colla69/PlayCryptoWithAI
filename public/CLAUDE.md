# Dashboard rules (public/)

@../.claude/rules/dashboard.md

Reminder: the dashboard is a single self-contained `index.html` (inline JS/CSS) fed by SSE.
Keep new UI additive — don't break existing panels, SSE event names, or layout contracts.
